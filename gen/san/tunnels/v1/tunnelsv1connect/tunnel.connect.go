// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: san/tunnels/v1/tunnel.proto

package tunnelsv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/wargasipil/san_tunnels/gen/san/tunnels/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TunnelServiceName is the fully-qualified name of the TunnelService service.
	TunnelServiceName = "san.tunnels.v1.TunnelService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TunnelServiceForwardProcedure is the fully-qualified name of the TunnelService's Forward RPC.
	TunnelServiceForwardProcedure = "/san.tunnels.v1.TunnelService/Forward"
	// TunnelServicePingProcedure is the fully-qualified name of the TunnelService's Ping RPC.
	TunnelServicePingProcedure = "/san.tunnels.v1.TunnelService/Ping"
)

// TunnelServiceClient is a client for the san.tunnels.v1.TunnelService service.
type TunnelServiceClient interface {
	// Forward is one call per tunnelled TCP connection. The first client message
	// must be `open`; every later one must be `data`.
	//
	// Requires HTTP/2. HTTP/1.1 cannot interleave a request and response body,
	// and this stream is full-duplex by nature.
	Forward(context.Context) *connect.BidiStreamForClient[v1.ForwardRequest, v1.ForwardResponse]
	// Ping is the duplex preflight probe. The client sends, reads a reply, then
	// sends again; a half-duplex path cannot complete that round trip, which
	// turns a silent mid-session hang into a loud failure at startup.
	Ping(context.Context) *connect.BidiStreamForClient[v1.PingRequest, v1.PingResponse]
}

// NewTunnelServiceClient constructs a client for the san.tunnels.v1.TunnelService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTunnelServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TunnelServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	tunnelServiceMethods := v1.File_san_tunnels_v1_tunnel_proto.Services().ByName("TunnelService").Methods()
	return &tunnelServiceClient{
		forward: connect.NewClient[v1.ForwardRequest, v1.ForwardResponse](
			httpClient,
			baseURL+TunnelServiceForwardProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("Forward")),
			connect.WithClientOptions(opts...),
		),
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+TunnelServicePingProcedure,
			connect.WithSchema(tunnelServiceMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// tunnelServiceClient implements TunnelServiceClient.
type tunnelServiceClient struct {
	forward *connect.Client[v1.ForwardRequest, v1.ForwardResponse]
	ping    *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Forward calls san.tunnels.v1.TunnelService.Forward.
func (c *tunnelServiceClient) Forward(ctx context.Context) *connect.BidiStreamForClient[v1.ForwardRequest, v1.ForwardResponse] {
	return c.forward.CallBidiStream(ctx)
}

// Ping calls san.tunnels.v1.TunnelService.Ping.
func (c *tunnelServiceClient) Ping(ctx context.Context) *connect.BidiStreamForClient[v1.PingRequest, v1.PingResponse] {
	return c.ping.CallBidiStream(ctx)
}

// TunnelServiceHandler is an implementation of the san.tunnels.v1.TunnelService service.
type TunnelServiceHandler interface {
	// Forward is one call per tunnelled TCP connection. The first client message
	// must be `open`; every later one must be `data`.
	//
	// Requires HTTP/2. HTTP/1.1 cannot interleave a request and response body,
	// and this stream is full-duplex by nature.
	Forward(context.Context, *connect.BidiStream[v1.ForwardRequest, v1.ForwardResponse]) error
	// Ping is the duplex preflight probe. The client sends, reads a reply, then
	// sends again; a half-duplex path cannot complete that round trip, which
	// turns a silent mid-session hang into a loud failure at startup.
	Ping(context.Context, *connect.BidiStream[v1.PingRequest, v1.PingResponse]) error
}

// NewTunnelServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTunnelServiceHandler(svc TunnelServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tunnelServiceMethods := v1.File_san_tunnels_v1_tunnel_proto.Services().ByName("TunnelService").Methods()
	tunnelServiceForwardHandler := connect.NewBidiStreamHandler(
		TunnelServiceForwardProcedure,
		svc.Forward,
		connect.WithSchema(tunnelServiceMethods.ByName("Forward")),
		connect.WithHandlerOptions(opts...),
	)
	tunnelServicePingHandler := connect.NewBidiStreamHandler(
		TunnelServicePingProcedure,
		svc.Ping,
		connect.WithSchema(tunnelServiceMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/san.tunnels.v1.TunnelService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TunnelServiceForwardProcedure:
			tunnelServiceForwardHandler.ServeHTTP(w, r)
		case TunnelServicePingProcedure:
			tunnelServicePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTunnelServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTunnelServiceHandler struct{}

func (UnimplementedTunnelServiceHandler) Forward(context.Context, *connect.BidiStream[v1.ForwardRequest, v1.ForwardResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("san.tunnels.v1.TunnelService.Forward is not implemented"))
}

func (UnimplementedTunnelServiceHandler) Ping(context.Context, *connect.BidiStream[v1.PingRequest, v1.PingResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("san.tunnels.v1.TunnelService.Ping is not implemented"))
}
